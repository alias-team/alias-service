이 문서는 팀의 Git 브랜치 운영 방식과 협업 규칙을 정의합니다.
> 본 문서는 프로젝트 진행에 따라 계속 업데이트 됩니다.

## 1. 브랜치 구조

| 브랜치 | 용도 | 직접 작업 |
| --- | --- | --- |
| `main` | 심사 및 최종 배포용 브랜치 | 금지 |
| `dev` | 검토가 끝난 작업을 통합하는 브랜치 | 금지 |
| `docs` | PRD, RFC, ADR, API 명세 등 문서 작업 | 허용 |
| `feature/*` | 백로그별 기능 개발 | 허용 |
| `fix/*` | 버그 수정 | 허용 |

`main`과 `dev`에는 직접 커밋하거나 푸시하지 않고 Pull Request로 병합합니다.

## 2. 브랜치 이름 규칙

백로그 항목 하나당 브랜치 하나를 생성합니다.

```
feature/{백로그번호}-{작업명}
fix/{백로그번호}-{작업명}
```

예시:

```
feature/P0-01-project-setup
feature/P0-02-home-page
feature/P1-01-ai-chat
fix/P0-03-api-error
```

- 영문 소문자와 숫자를 사용합니다.
- 단어 사이는 하이픈()으로 구분합니다.
- 서로 다른 백로그 작업을 하나의 브랜치에 섞지 않습니다.

## 3. 기능 개발 순서

### 3-1. 최신 `dev` 받기

```
git switch dev
git pull origin dev
```

### 3-2. 작업 브랜치 생성하기

```
git switch -c feature/P0-02-home-page
```

버그 수정이라면 `fix/*` 브랜치를 생성합니다.

```
git switch -c fix/P0-03-api-error
```

### 3-3. 작업하고 커밋하기

```
git add .
git commit -m "feat: 홈 화면 구현"
```

### 3-4. 원격 저장소에 올리기

```
git push -u origin feature/P0-02-home-page
```

### 3-5. Pull Request 생성하기

- 병합 방향: `feature/*` 또는 `fix/*` → `dev`
- PR 설명에 작업한 내용과 확인 방법을 작성합니다.
- 팀원 확인 후 `dev`에 병합합니다.
- 병합이 끝난 작업 브랜치는 삭제합니다.

### 3-6. 다음 백로그 시작하기

다음 작업도 최신 `dev`에서 새로운 브랜치를 생성합니다.

```
git switch dev
git pull origin dev
git switch -c feature/P1-01-next-task
```

## 4. 문서 작업 순서

PRD, RFC, ADR, API 명세 등 문서 작업은 `docs` 브랜치에서 진행합니다.

작업 전 `dev`의 최신 변경 사항을 반영합니다.

```
git switch docs
git pull origin docs
git merge dev
```

문서 작업이 끝나면 커밋과 푸시 후 `docs` → `dev` Pull Request를 생성합니다.

## 5. 커밋 메시지 규칙

```
타입: 작업 내용
```

| 타입 | 용도 | 예시 |
| --- | --- | --- |
| `feat` | 새로운 기능 | `feat: 로그인 화면 구현` |
| `fix` | 버그 수정 | `fix: API 오류 처리` |
| `docs` | 문서 변경 | `docs: API 명세 추가` |
| `style` | 코드 동작과 무관한 형식 수정 | `style: 버튼 간격 수정` |
| `refactor` | 기능 변화 없는 코드 개선 | `refactor: 인증 로직 분리` |
| `test` | 테스트 추가 및 수정 | `test: 로그인 API 테스트 추가` |
| `chore` | 설정 및 기타 작업 | `chore: 환경변수 예시 추가` |

커밋 하나에는 가능하면 하나의 작업 단위만 포함합니다.

## 6. Pull Request 규칙

PR 제목은 커밋 메시지와 같은 형식을 사용합니다.

```
feat: 홈 화면 구현
```

PR 본문에는 다음 내용을 작성합니다.

```
## 작업 내용
- 구현하거나 수정한 내용

## 확인 방법
- 실행 및 테스트 방법

## 참고 사항
- 미완성 부분, 알려진 오류, 리뷰가 필요한 내용
```

병합 전 다음 항목을 확인합니다.

- [ ]  로컬에서 정상적으로 실행되는가?
- [ ]  핵심 기능을 직접 확인했는가?
- [ ]  API 키와 개인정보가 포함되지 않았는가?
- [ ]  `.env`가 커밋되지 않았는가?
- [ ]  배포에 영향을 주는 변경 사항을 팀원에게 공유했는가?

## 7. 최종 배포

기능 통합과 검증이 끝나면 `dev`에서 `main`으로 Pull Request를 생성합니다.

```
dev → main
```

`main`에는 실제 배포 링크와 연결된 최종 코드만 유지합니다. 해커톤 제출 이후에는 저장소를 수정하지 않습니다.

## 8. 핵심 원칙

1. 백로그 항목 하나당 작업 브랜치 하나를 생성합니다.
2. 새로운 작업은 항상 최신 `dev`에서 시작합니다.
3. `main`과 `dev`에는 직접 푸시하지 않습니다.
4. 완료된 작업 브랜치는 `dev` 병합 후 삭제합니다.
5. API 키, 비밀번호, 개인정보는 GitHub에 올리지 않습니다.
6. 해커톤 제출 전 배포 브랜치와 제출 브랜치가 모두 `main`인지 확인합니다.